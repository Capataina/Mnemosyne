# papers/ — 24 academic-paper source notes from the 2026-04-26 research run

One note per paper, uniform frontmatter (`source_type: paper`, real publication
`date_published`, `hype_score` mostly 0-1 — this is the corpus's highest-trust
subfolder). Clusters: ANN indexing (HNSW, DiskANN, ScaNN, ann-benchmarks,
PACMann), the CLIP/SigLIP/DINOv2 model family, retrieval diversity (MMR, DPP,
Spotify/Pinterest), FHE (CKKS, Wally), quantisation and dedup. Frozen; parent
`../CLAUDE.md` carries the trust caveat — re-fetch the paper before a number
here drives a decision.
