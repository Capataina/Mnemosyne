# projects/ — 35 shipped-project/crate source notes from the 2026-04-26 run

One note per project (`source_type: shipped-project`): vector search (faiss,
qdrant, usearch, lancedb, instant-distance), ML runtimes (candle, burn/tract,
ort, hf-tokenizers, optimum), comparable apps (immich, eagle, hydrus, czkawka,
photoprism, spacedrive), FHE (tfhe-rs, zama, swift-homomorphic-encryption), and
Rust/Tauri ecosystem crates. `date_published: 2026-04` means "as seen at run
time", not a release date. Frozen; parent `../CLAUDE.md` carries the trust
caveat, and crate/API claims here are 15 months old — check current versions.
